<li>
	{{burger_name}}
	{{#unless devoured}}
	<button class="change-sleep" data-id="{{id}}">
		NOT DEVOURED
	</button>
	{{/unless}}
</li>

{{!-- <button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
--}}